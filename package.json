{
  "author": "Hiroya Kubo <hiroya@cuc.ac.jp>",
  "name": "node-libpafe",
  "description": "libpafe wrapper addon",
  "version": "0.0.1",
  "homepage": "https://github.com/kubohiroya/node-libpafe",
  "repository": {
    "type": "git",
    "url": "git://github.com/kubohiroya/node-libpafe.git"
  },
  "engines": {
    "node": "~v0.10.25"
  },
  "dependencies": {
  },
  "devDependencies": {},
    "scripts":{
    "preinstall": "(cd deps/unix/libusb-1.0.18; ./configure --prefix=/tmp && make install && cp /tmp/include/libusb-1.0/libusb.h  /tmp/include; cp /tmp/lib/libusb.a /tmp/lib/libusb-1.0.a); (cd deps/unix/libusb-compat-0.1.5; export LIBUSB_1_0_CFLAGS='-I/tmp/include -L/tmp/lib'; ./configure --prefix=/tmp --includedir=/tmp/include --libdir=/tmp/lib && make install); (cd deps/unix/libnfc-1.7.0 ; export libusb_CFLAGS='-I/tmp/include -L/tmp/lib';  export LDFLAGS='-lusb-1.0';./configure --includedir=/tmp/include --libdir=/tmp/lib && make); node-gyp rebuild"
  }
}
